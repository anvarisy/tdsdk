package com.fazpass.td;

import androidx.core.content.FileProvider;

public class Sharing extends FileProvider {
    @Override
    public boolean onCreate() {
        return super.onCreate();
    }
}
